# Agent Factory — Environment Template (sanitize before use)
# Copy to .env (not committed) and fill with your own keys.
# NOTE: .gitignore already excludes .env

# OpenAI keys (scoped by role, optional). If not set, OPENAI_API_KEY is used as fallback for all.
OPENAI_API_KEY=
OPENAI_KEY_MANAGER=
OPENAI_KEY_WORKER=
OPENAI_KEY_SAFE=

# Standardized ChatGPT model suite (Phase 38.5)
OPENAI_MODEL_REFLECTIVE=gpt-4o-mini
OPENAI_MODEL_ETHICS=gpt-4-turbo
OPENAI_MODEL_SUMMARY=gpt-4o

# API security for Archy server (JWT secret)
ARCHIVIST_API_SECRET=

# Optional OSS model endpoint (e.g., Ollama)
GPT_OSS_ENDPOINT=http://localhost:11434/api/generate

# Other providers (examples)
GOOGLE_API_KEY=
ANTHROPIC_API_KEY=
SERPER_API_KEY=

# Local Python path for servers (Windows example)
# PYTHONPATH=C:\Users\benny\IdeaProjects\agent-factory\src
